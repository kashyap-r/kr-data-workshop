
🚀 Step 1: Install Dependencies

Inside project root:

    pip install fastapi uvicorn
    pip freeze > requirements.txt

⚙️ Step 2: Build Incrementally (File by File)

1. app/main.py (entry point)
2. app/models.py (Schemas) 
3. 
